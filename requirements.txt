# Runtime dependencies for the app (no TensorFlow required).
# TensorFlow is only needed by train_model_deep_learning.py and should be
# installed in a separate training environment on supported hardware.
opencv-python>=4.8.0
numpy>=1.26.0
scipy>=1.11.0
scikit-learn>=1.3.0
pandas>=2.1.0
joblib>=1.3.0
mediapipe>=0.10.0
PyQt5>=5.15.0
pyttsx3>=2.90
